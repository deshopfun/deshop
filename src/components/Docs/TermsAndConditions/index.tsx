import { Box, Card, CardContent, Container, Typography } from '@mui/material';

const DocsTermsAndConditions = () => {
  return (
    <Container>
      <Card>
        <CardContent>
          <Typography>DocsPrivacyPolicy</Typography>
        </CardContent>
      </Card>
    </Container>
  );
};

export default DocsTermsAndConditions;
