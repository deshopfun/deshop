import { Box, Card, CardContent, Container, Typography } from '@mui/material';

const DocsFees = () => {
  return (
    <Container>
      <Card>
        <CardContent>
          <Typography>DocsFees</Typography>
        </CardContent>
      </Card>
    </Container>
  );
};

export default DocsFees;
