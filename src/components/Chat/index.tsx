import { Box, Container, Typography } from '@mui/material';

const Chat = () => {
  return (
    <Container>
      <Typography variant="h6">Chat</Typography>
    </Container>
  );
};

export default Chat;
