import { Box, Container, Typography } from '@mui/material';

const Live = () => {
  return (
    <Container>
      <Typography variant="h6">Livestreams</Typography>
    </Container>
  );
};

export default Live;
